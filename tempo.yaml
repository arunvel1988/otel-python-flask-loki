server:
  http_listen_port: 3200
  grpc_listen_port: 4317

receivers:
  otlp:
    protocols:
      grpc:
      http:

distributor:
  receivers: [otlp]

ingester:
  trace_idle_period: 5m

storage:
  trace:
    backend: local
    local:
      path: /tempo
