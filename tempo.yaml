server:
  http_listen_port: 3200
  grpc_listen_port: 4317
  log_level: debug

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
        http:

storage:
  trace:
    backend: local
    local:
      path: /tempo

compactor:
  compaction_interval: 1m
